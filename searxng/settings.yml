use_default_settings: true

general:
  debug: false
  instance_name: "ROZHAN Search"

search:
  formats:
    - html
    - json

server:
  limiter: false
  public_instance: false
  image_proxy: false
  port: 10000
  bind_address: "0.0.0.0"

